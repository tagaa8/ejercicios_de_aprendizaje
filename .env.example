# Variables de ejemplo (no necesarias para correr localmente)
# Copia este archivo a `.env` y ajusta si deseas.

# Lista de orígenes permitidos (CORS) separada por comas, por ejemplo:
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Para usar Postgres en lugar de SQLite, podrías definir algo como:
# DATABASE_URL=postgresql+psycopg2://user:password@host:5432/dbname

